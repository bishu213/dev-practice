/* paramenter are can't change and 
arguments can be change
*/

function sum(n1, n2) { //n1 and n2 are parameters
    console.log(`sum = ${n1+n2}`);
}
sum(12, 32); //12 and 32 are arguments
sum(21, 14); //21 and 14 are arguments

let x =10; //x is variable
let y= 12; //y is variable
sum(x, y) //x, y are arguments